function drawPartyAsk() {
  background(80, 20, 60);

  fill(240);
  textSize(22);
  text(
    "Maya smiles.\nShe says yes.\nThe energy feels electric.\n\nThe day of the date arrives...",
    width / 2,
    height / 2,
  );

  drawButton(width / 2, 380, 260, 60, "Continue");
}
